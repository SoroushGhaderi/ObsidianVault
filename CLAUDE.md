# Claude Code Instructions

Read `llm/SCHEMA.md` fully before any vault task. It is the authoritative operating contract.

At session start, inspect `wiki/index.md`, the most recent monthly file under `log/` when one exists, and the Git status. Preserve unrelated changes and never modify files under `raw/`.

Agent skills are installed globally. Do not create or maintain skill implementations inside this vault.
